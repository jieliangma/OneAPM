language: objective-c
# podfile: Example/Podfile
before_install:
    - gem install cocoapods --quiet
    - pod lib lint
script:
    - xctool -workspace Example/OneAPM-Example.xcworkspace -scheme OneAPM-Example -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
